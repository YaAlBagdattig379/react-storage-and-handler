const add = (first,second) =>{ // function
     return first + second;
}
// export default add;
const multiply = (first,second)=>{ // function
    return first * second;
}

export { add, multiply};// export multiple function by this way
